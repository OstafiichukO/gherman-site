# Runtime-аудит продакшену, частина 2

**Дата прогону:** 2026-09-06 (перша частина — 2026-09-05, див. `AUDIT-RUNTIME-PLAYWRIGHT.md`)  
**Ціль:** `https://gherman.com.ua`

Друга частина закриває те, чого не було в першій: усі шість категорій каталогу, решта типів сторінок об'єктів, суцільна перевірка посилань, контраст і клавіатура, матриця viewport-ів, троттлінг CPU, заголовки й кешування.

> **Важливо:** між двома прогонами прод було передеплоєно, і частина знахідок першого звіту вже закрита, а одна — породила нову регресію. Розділ 1 фіксує різницю, розділ 9 — статус кожної знахідки з першого звіту.

---

## 1. Що змінилось у проді між прогонами

| Показник | 05.09 | 06.09 | Зміна |
|---|---:|---:|---|
| `index.html` (розпакований) | 5 269 348 Б | **397 779 Б** | −92 % |
| `index-desktop-preview.html` | 5 461 083 Б | **483 383 Б** | −91 % |
| base64-зображень у HTML | 31 (4.65 МБ) | **0** | усі винесені у файли |
| Мобільний перегляд, по мережі | 3 598 КБ | **1 557 КБ** | −57 % |
| Десктопний перегляд (два документи) | 7 286 КБ | **1 659 КБ** | −77 % |
| `canonical` десктопної версії | `gherman-agency.netlify.app` | **`gherman.com.ua`** | виправлено |
| `manual_listings` для `anon` | 401 | **200** | GRANT видано |

Зображення тепер лежать окремими файлами з хешем у назві (`/images/img-<hash>.jpg`) — рівно те, що рекомендував розділ 2.2 першого звіту. Три найдорожчі пункти закриті.

### 1.1 ⚠️ Репозиторій не збігається з продом

`origin/main` досі містить старі 5-мегабайтні `index.html` та `index-desktop-preview.html`; `sitemap.xml` теж різний (123 413 Б у проді проти 122 333 Б у репо). Тобто прод зібрано не з цієї гілки.

Наслідок практичний: будь-який статичний аудит по репозиторію зараз описує **не той код, що працює**. Це варто полагодити раніше за решту — інакше кожен наступний аудит (і людський, і автоматичний) даватиме хибну картину.

---

## 2. P0 — регресія, яку приніс новий деплой

### 2.1 CLS головної зріс у 100 разів

Зображення винесли з HTML у файли, але **не додали їм розмірів**. Поки картинки були base64, вони приїжджали разом із розміткою і нічого не стрибало. Тепер вони вантажаться окремо, місце під них не зарезервоване — і сторінка перебудовується вже після першого малювання.

| | 05.09 (base64) | 06.09 (файли) |
|---|---:|---:|
| CLS десктопної головної | 0.008 | **0.82** |
| CLS мобільної головної | 0.001 | **0.48** |

Поріг «добре» за Core Web Vitals — 0.1. Обидва значення тепер у червоній зоні (>0.25).

Зсув **один і великий**, джерело локалізоване точно:

```
десктоп: +0.8245 на 610 мс  ← DIV#search-row.search-row | DIV#search-card.search-card | DIV#search-side-text
мобільна: +0.4843 на 1036 мс ← DIV#search-card.search-card | svg | DIV#home-intro-top
```

Це головна картка пошуку — той самий блок із «Квартира / Будинок / Земля», бюджетом і кнопкою «Показати об'єкти». Її зіштовхує вниз hero-зображення `img-e545f33607.jpg`, коли нарешті завантажується. Тобто стрибає рівно той елемент, заради якого користувач прийшов, і стрибає через півсекунди — саме тоді, коли він уже тягнеться пальцем.

**13 із 14 зображень головної не мають ні `width`/`height`, ні `aspect-ratio`**, і в тих самих 13 немає `loading="lazy"`.

**Фікс:** проставити `width`/`height` (або `aspect-ratio` у CSS) усім `<img>`. Це один атрибут на тег і повертає CLS до ~0.

### 2.2 Зображення вантажаться у 3–7 разів більшими за місце показу

Заміряно на реальному рендері нового білду:

| Файл | Натуральний розмір | Показується (десктоп) | Показується (мобільна) |
|---|---|---|---|
| `img-e545f33607.jpg` (hero) | 1100×907 | **1440×1187 — розтягнуто вгору** | 390×322 |
| `img-27ec1a1bf0.jpg` | 900×1124 | 388×484 | 350×437 |
| `img-d568ae3457.jpg` | 900×900 | 182×434 | 167×125 |
| `img-25e417e081.jpg` | 900×900 | 182×208 | **167×125** |
| `img-bd336387a4.png` | 220×172 | 120×93 | **46×36** |

Hero-картинка при цьому **менша за місце показу на десктопі** (1100 px натуральних розтягуються на 1440) — тобто на великих екранах вона ще й розмита.

Зображення — це 1 475 КБ з 1 557 КБ усього перегляду (95 %). Формати — JPEG/PNG, без WebP/AVIF, без `srcset`/`sizes`. Підгонка під фактичні розміри + WebP тут дає більший виграш, ніж усе інше разом узяте.

### 2.3 Хешовані імена файлів без вічного кешу

Файли названі за вмістом (`img-<hash>.jpg`) — це саме той випадок, для якого існує `max-age=31536000, immutable`. Фактично віддається:

```
cache-control: public,max-age=0,must-revalidate
```

ETag працює — повторний запит дає `304` і 0 байт. Тож це не перезавантаження контенту, а **12 зайвих умовних запитів на кожен повторний перегляд**. Те саме на `/sitemap.xml`, `og-cover.jpg` і статичних HTML. Сторінки об'єктів, які генерує edge-функція, отримують нормальний `max-age=3600`.

---

## 3. Каталог: що насправді можна переглянути

### 3.1 Лічильник «варіантів» показує захардкожене число

У розмітці:

```html
<span class="price" id="budget-display">до 45 000$</span>
<span class="count" id="variants-count">279 варіантів</span>
```

Заміряно в браузері:

| Дія | Що показує | Запитів до бази |
|---|---|---|
| Завантаження сторінки | `279 варіантів` | **0** |
| Перемикання на «Будинок» | `279 варіантів` | **0** |
| Зміна поля «Кімнат» = 2 | `59 варіантів` | 1 |

Функція `updateVariantsCount()` жива й рахує правильно — але на завантаженні її **ніхто не викликає**, і перемикання категорії її теж не тригерить. Єдиний працюючий тригер — `addEventListener('input')` на полі «Кімнат» (рядок 4779).

Тобто перше, що бачить кожен відвідувач під написом «до 45 000$», — це число з розмітки, яке ніяк не пов'язане з фільтром. Реальні числа для стану за замовчуванням (квартири, ≤ 45 000 $):

* у базі під фільтр підпадає **51** об'єкт;
* із них мають фото і тому можуть бути показані — **18**;
* десктопна стрічка після натискання «ПОКАЗАТИ ОБ'ЄКТИ» відмальовує **16 карток**.

**279 проти 16 на екрані.** Це головний конверсійний віджет сторінки.

### 3.2 83 % каталогу неможливо переглянути — немає фото

Стрічка відсіює об'єкти без фото (`index.html:4382`), а лічильник — ні. Розрив по категоріях:

| Категорія | У базі | З фото (тобто показуються) |
|---|---:|---:|
| Квартири | 353 | **88** |
| Будинки | 280 | **23** |
| Земля | 61 | **4** |
| Новобудови (`house_type`) | 155 | 36 |
| Комерція | 1 | **0** |
| Оренда | 0 | 0 |
| **Разом** | **685** | **115** |

570 з 685 об'єктів (83 %) не мають жодного фото. Це не помилка коду — це стан даних у CRM, але саме він визначає, що бачить клієнт. Сторінки об'єктів для них генеруються (вони в sitemap), просто в каталозі їх немає.

### 3.3 Порожні категорії: на мобільній дві, на десктопі три

Прокликано всі шість пілюль на обох версіях:

| Категорія | Мобільна | Десктопна |
|---|---|---|
| Квартира | 10 карток | 16 карток |
| Будинок | 10 карток | 5 карток |
| Земля | 4 картки | 2 картки |
| Новобудова | 10 карток | **порожньо** |
| Комерція | **порожньо** | **порожньо** |
| Оренда | **порожньо** | **порожньо** |

Причини різні:

* **Новобудова** — мобільна питає правильно (`house_type=ilike.*новобудова*`), десктопна питає `obj_cat=ilike.*newbuild*`. Значення `newbuild` у колонці `obj_cat` не існує взагалі (там лише `flat`, `house`, `land`, `commerce`), тому десктопна версія завжди отримує порожній список.
* **Комерція** — у базі рівно 1 комерційний об'єкт, і той без фото. Його сторінка `/listings/bdc0e2f0-…` віддає 404 «Цей об'єкт вже продано».
* **Оренда** — без змін із першого звіту: запит по неіснуючій колонці `type` (400) плюс `manual_listings?listing_type=eq.rental`, який тепер відповідає `200`, але повертає `[]` — рядків оренди в базі немає.

### 3.4 Десктопна версія мовчки застосовує бюджет, мобільна — ні

Десктопні запити категорій містять `&price=lte.45000` (значення повзунка за замовчуванням), мобільні — ні. Тому на однакових фільтрах дві версії сайту показують різні набори об'єктів, і десктопний гість до дотику до повзунка бачить лише дешевий сегмент.

---

## 4. Решта типів сторінок

Прогнано 10 додаткових сторінок × 2 viewport-и.

### 4.1 CLS на сторінках об'єктів — системний

| Сторінка | CLS мобільна | CLS десктоп |
|---|---:|---:|
| Квартира з фото | **0.47** | 0.16 |
| Квартира без фото | 0 | 0.10 |
| Будинок з фото | 0 | 0.18 |
| Земля без фото | 0 | 0.10 |
| Квартира (з першого звіту) | **0.62** | 0.22 |

Причина всюди одна: єдине фото без `width`/`height` і з `loading="lazy"`, хоча саме воно є LCP-елементом. Правити в одному місці — шаблон `netlify/edge-functions/listing-pages.js:84`.

### 4.2 Об'єкти без фото показують чужу картинку як свою

Для об'єкта без фото edge-функція підставляє `og-cover.jpg` — загальну обкладинку агенції — і виводить її на всю ширину там, де мала б бути фотографія об'єкта, з `alt`, що дублює заголовок оголошення. Клієнт бачить сторінку «3-кімнатна Квартира — 60 000 $ · Проспект» із фотографією, яка не має до цієї квартири стосунку. Це стосується **570 із 685** сторінок.

Чесніше — або плейсхолдер із явним підписом «фото готується», або взагалі без блоку фото.

### 4.3 Відмінки в заголовках

**Сторінки об'єктів.** `listing-pages.js:132` формує префікс жорстко у знахідному відмінку:

```js
const roomsWord = (r.rooms && cat === 'flat') ? `${r.rooms}-кімнатну ` : '';
```

Результат — `1-кімнатну Квартира — 34 000$ · Руська` на **всіх 353** сторінках квартир. Це текст, який Google показує у видачі.

**Статичні сторінки.** У 28 із 88 сторінок (`rayony/` + `baza-znan/`) неправильна форма числівника в `<title>`: «3 варіантів» замість «3 варіанти», «21 варіантів» замість «21 варіант», «102 варіантів» замість «102 варіанти». Плюс 44 сторінки районів починають `<title>` з родового відмінка — «Квартир у районі центр…», «Будинків у районі Роша…» — хоча в `<h1>` на тих самих сторінках правильно («Квартири у районі центр»).

Прикро те, що правильна функція відмінювання в коді вже є (`pluralizeVariants`, `index.html:2951`) — генератор статики просто нею не користується.

### 4.4 Структурних даних немає там, де вони найбільше потрібні

| Тип сторінки | К-сть | `ld+json` |
|---|---:|---|
| Об'єкт | 685 | ✅ `RealEstateListing` + `Offer` |
| Стаття-FAQ | 33 | ✅ `FAQPage` |
| Район | 55 | ❌ немає |
| Сторінка за кімнатністю | 4 | ❌ немає |
| Гід | 1 | ❌ немає |

Сторінки районів і кімнатності — це списки з цінами, тобто ідеальні кандидати на `ItemList` / `CollectionPage`.

### 4.5 Сторінки за кімнатністю живі, але гід веде на них не туди

`/baza-znan/1-kimnatni-kvartyry-chernivtsi.html` працює нормально: 63 внутрішні посилання, 2 228 символів тексту. А гід (`gid-po-neruhomosti-chernivtsi.html` — хабова сторінка, є в sitemap) посилається на них за неіснуючими шляхами. Детально — розділ 6.

---

## 5. Доступність

### 5.1 Контраст

Порахований на реальному рендері за WCAG (елементи поверх фотографій виключені — там фон не обчислити надійно).

| Сторінка | Провалів | Найгірше |
|---|---:|---|
| Головна (мобільна) | 9 | золотий `#C9A961` на кремовому — **1.96:1** (треба 4.5) |
| Головна (десктоп) | 10 | те саме, плюс `.da-eyebrow`, `.cd-cap`, `.da-more` |
| Гід | 2 | `p.sub` `#8A7D70` — 3.81:1 |
| Об'єкт / Район / Стаття | по 1 | футер `#999999` на `#FBF9F5` — **2.71:1** |

Три системні випадки:

1. **Золотий `#C9A961` на кремовому `#F1EFEB` — 1.96:1.** Використовується для акцентних написів і посилань («Написати боту →», «ЩО КАЖУТЬ КЛІЄНТИ», «Залишити відгук у Google»). Треба ≥4.5:1. Найсвітліший відтінок того самого золотого, що проходить, — **`#7E6A3D`** (4.55:1); для великого тексту (≥3:1) вистачить `#A0874D`. Альтернатива — лишити `#C9A961` тільки для декору, не для тексту.
2. **`#A88C5C` на білому при 10 px — 3.2:1.** Це підписи фільтрів «РАЙОН», «КІМНАТ», «БЮДЖЕТ», «БІЛЬШЕ ФІЛЬТРІВ» — найфункціональніший текст сторінки, і водночас найдрібніший і найблідіший. Проходить від **`#89724B`** (4.59:1).
3. **Футерний `#999999` — 2.71:1 на кожній підсторінці** (об'єкт, район, стаття, гід — це 770+ сторінок). Проходить від **`#727272`** (4.57:1).

### 5.2 Клавіатура і фокус

Добре: **жодного `tabindex > 0`**, у жодного фокусованого елемента не прибрано видимий фокус (`outline`/`box-shadow` на місці). Це вже краще за середній рівень.

Погано: **у модалок немає фокус-пастки.** З відкритою модалкою об'єкта Tab виводить фокус на елементи під нею — з 12 кроків 5 опинилися за межами модалки, причому вже перші три:

```
BUTTON [поза модалкою] → BUTTON [поза модалкою] → A [поза модалкою] → INPUT#rooms-filter [поза модалкою]
```

Разом із тим, що **Escape модалку не закриває** (знахідка 1.4 першого звіту, досі відтворюється), користувач клавіатури опиняється в пастці навпаки: фокус пішов у фон, а закрити вікно з клавіатури нічим. Клік по підкладці закриває — це працює.

---

## 6. Посилання: суцільна перевірка

Перевірено **699 URL із sitemap** і **614 внутрішніх посилань** з усіх 700 статичних HTML. Набір посилань зібрано з репозиторію, а перевірено вже проти живого сайту; з огляду на 1.1 прод може містити ще й посилання, яких у репо немає.

### 6.1 Гід посилається на чотири неіснуючі сторінки

| Посилання в гіді | Код | Де файл насправді |
|---|---:|---|
| `/kimnatnist/1-kimnatni-kvartyry-chernivtsi.html` | **404** | `/baza-znan/1-kimnatni-kvartyry-chernivtsi.html` |
| `/kimnatnist/2-kimnatni-kvartyry-chernivtsi.html` | **404** | `/baza-znan/2-…` |
| `/kimnatnist/3-kimnatni-kvartyry-chernivtsi.html` | **404** | `/baza-znan/3-…` |
| `/novobudovy/novobudovy-chernivtsi.html` | **404** | `/baza-znan/novobudovy-chernivtsi.html` |

Це єдині биті внутрішні посилання на сайті — і всі чотири на хабовій сторінці, яка сама є в sitemap. Виправляється заміною префікса шляху. Побіжно: `4plus-kimnatni-kvartyry-chernivtsi.html` існує, але з гіда на неї не веде нічого.

### 6.2 Sitemap

2 URL із 699 віддають 404 — ті самі продані об'єкти з першого звіту. Решта 697 живі.

### 6.3 404 для об'єктів зроблена добре — це поправка до першого звіту

У першому звіті я написав, що 404 на сайті — це стандартна англійська заглушка Netlify. Це вірно для `/baza-znan/`, `/rayony/`, `/listings/` і довільних неіснуючих шляхів, але **не** для сторінок об'єктів: edge-функція віддає власну сторінку — українською, з брендуванням, `noindex, follow` і кнопкою назад у каталог:

> «Цей об'єкт вже продано або знятий з продажу. Але в каталозі GHERMAN є багато інших актуальних…»

Тобто клієнт, що прийшов за проданим об'єктом, потрапляє куди треба. Проблема лишається тільки в sitemap (даремно витрачений краулінговий бюджет і помилки в Search Console) і в тому, що загальний 404 для решти шляхів так і лишився англійською заглушкою хостингу.

### 6.4 Зовнішні посилання перевірити не вдалося

17 зовнішніх посилань (Telegram, Instagram, Facebook, TikTok, Google Maps, `avtostrahovka.com.ua`, `wa.me`) недоступні з середовища прогону — вихідний трафік обмежений політикою егресу, і всі вони однаково впали з мережевою помилкою, а не з кодом відповіді. **Це не означає, що вони биті** — просто цей аудит про них нічого сказати не може. Їх треба перевірити з нормального каналу.

---

## 7. Viewport-и: планшети отримують телефонну версію

Прогнано 10 розмірів екрана.

| Viewport | Куди потрапляє | Ширина контенту |
|---|---|---:|
| 320 / 360 / 390 (телефони) | `/` | на всю ширину ✅ |
| **844×390 (телефон горизонтально)** | `/` | **480 px** |
| **768 (iPad)** | `/` | **480 px** |
| **820 (iPad Air)** | `/` | **480 px** |
| **1024 (iPad Pro)** | `/` | **480 px** |
| 1280 / 1440 / 1920 | `/index-desktop-preview.html` | на всю ширину ✅ |

Усе між 640 і 1024 px отримує мобільну версію, зафіксовану колонкою **480 px** посеред порожнього поля, тоді як шапка й нижня панель розтягнуті на всю ширину. Проміжного брейкпоїнта немає взагалі.

Це ширша версія знахідки 1.5 першого звіту: справа не лише в тачскрін-ноутбуках — це **всі планшети та будь-який телефон, повернутий горизонтально**. Для сайту нерухомості, де фото дивляться саме горизонтально, це помітний сегмент.

Хороша новина: **горизонтального переповнення немає на жодному з 10 розмірів** — верстка ніде не ламається, просто не використовує доступну ширину.

---

## 8. Швидкість на слабкому пристрої та заголовки

### 8.1 Троттлінг CPU

Мобільна головна, емуляція повільнішого процесора:

| CPU | `load` | FCP | LCP | Довгі задачі | Перемикання категорії |
|---|---:|---:|---:|---:|---:|
| ×1 | 1 035 мс | 512 | 892 | 2 шт / 121 мс | 833 мс |
| ×4 | 1 150 мс | 776 | 1 076 | 3 шт / 393 мс | 748 мс |
| ×6 | 1 661 мс | 792 | 1 220 | 5 шт / 661 мс | 750 мс |

Після виносу base64 сторінка стала помірно вимогливою: навіть при ×6 головний потік блокується сумарно на 0.66 с. Це прийнятно. Вузьке місце тепер не процесор, а 1.4 МБ зображень і відсутність у них розмірів (розділ 2).

### 8.2 Заголовки безпеки

```
strict-transport-security: max-age=31536000
```

І це все. Немає `Content-Security-Policy`, `X-Frame-Options` / `frame-ancestors`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`.

Два наслідки, які варто закрити:

* сайт можна вкласти в `<iframe>` на чужому домені (клікджекінг на формах заявок);
* без `Referrer-Policy` повний URL сторінки йде у `Referer` третім сторонам — а сторінка ходить на `ipapi.co`, тож туди потрапляють і адреси з `?obj=<id>`.

Обидва лікуються одним файлом `_headers` або `netlify.toml` — там же, де варто виставити `immutable` для `/images/*` (розділ 2.3).

---

## 9. Статус знахідок першого звіту

| # | Знахідка | Статус на 06.09 |
|---|---|---|
| 1.1 | Ексклюзиви: 401 і підставна картка «Особняк, район Роша» | ✅ **Виправлено.** GRANT видано, блок показує реальний об'єкт («Роша клуб 3», 9 фото). Захардкожена заглушка в коді лишилась і спрацює при будь-якому наступному збої |
| 1.2 | Оренда порожня | ⚠️ **Частково.** `manual_listings` тепер 200, але повертає `[]`; запит `type=eq.rent_offer` досі 400 (3 місця × 2 файли) |
| 1.3 | `faq_questions`: немає INSERT для `anon`, є зайвий SELECT | ❌ **Без змін.** INSERT — 401, SELECT — 200 |
| 1.4 | Escape не закриває модалки | ❌ **Без змін.** Плюс з'ясувалось, що немає й фокус-пастки (5.2) |
| 1.5 | Тачскрін-ноутбук отримує мобільну версію | ❌ **Без змін.** Ширше, ніж здавалось: усі планшети й альбомна орієнтація (7) |
| 2.1 | Десктоп качає обидва документи | ⚠️ **Пом'якшено.** Подвійне завантаження лишилось, але коштує вже 182 КБ, а не 7.3 МБ |
| 2.2 | 93 % HTML — base64 | ✅ **Виправлено.** Винесено у файли, −92 % ваги HTML. Але породило регресію CLS (2.1) і лишило зображення непідігнаними (2.2) |
| 2.3 | Сторінка об'єкта: CLS 0.62, lazy на LCP | ❌ **Без змін.** Підтверджено на 5 типах об'єктів (4.1) |
| 2.4 | Каталог без ліміту й пагінації | ❌ **Без змін** |
| 3.1 | `user-scalable=no` | ❌ **Без змін** |
| 3.2–3.7 | Тап-цілі, дрібний шрифт, поля без label, `href="#"`, `noopener` | ❌ **Без змін** (18 / 27 / 3 / 12 на мобільній) |
| 4.1 | `canonical` десктопної на netlify-домен | ✅ **Виправлено** |
| 4.2 | `/baza-znan/`, `/rayony/`, `/listings/` → 404 | ❌ **Без змін** |
| 4.3 | Sitemap розсинхронізований | ❌ **Без змін** (2 мертві, 78 відсутніх) |
| 4.4 | `/?obj=` і `/listings/` — два індексовані дублікати | ❌ **Без змін** |
| 4.5 | Сторінки районів обрізані на 30 | ❌ **Без змін** |
| 4.6 | Стаття — глухий кут без перелінковки | ❌ **Без змін** |
| 5.1 | Аналітика не бачить десктоп | ❌ **Без змін** |
| 5.2 | `ipapi.co` до згоди | ❌ **Без змін** |
| 5.3 | Що видно через anon-ключ | ⚠️ **Уточнено.** Сторінок для `deal_type=buy` не існує (404) і в sitemap їх немає — витік лише через REST API, не через сайт |

---

## 10. Пріоритети після цього прогону

| # | Дія | Розділ | Чому першим |
|---|---|---|---|
| 1 | `width`/`height` усім `<img>` на головній | 2.1 | CLS 0.82 → ~0; регресія свіжа, править одним атрибутом |
| 2 | Синхронізувати репозиторій із продом | 1.1 | інакше наступний аудит знову дивиться не туди |
| 3 | Викликати `updateVariantsCount()` на завантаженні й при зміні категорії | 3.1 | «279» проти 16 карток на екрані |
| 4 | `width`/`height` + прибрати `lazy` з фото об'єкта | 4.1 | 685 сторінок, CLS до 0.62 |
| 5 | Виправити шлях `/kimnatnist/` → `/baza-znan/` у гіді | 6.1 | 4 биті посилання на хабовій сторінці |
| 6 | Десктопний запит новобудов: `obj_cat=newbuild` → `house_type` | 3.3 | цілий розділ порожній на десктопі |
| 7 | `_headers`: `immutable` для `/images/*`, `X-Frame-Options`, `Referrer-Policy` | 2.3, 8.2 | один файл закриває три пункти |
| 8 | Відмінки: `-кімнатну` в edge-функції, `pluralizeVariants` у генераторі статики | 4.3 | 353 + 28 сторінок у видачі Google |
| 9 | Брейкпоїнт для 640–1024 px | 7 | усі планшети й альбомна орієнтація |
| 10 | Контраст: золотий текст і футерний `#999` | 5.1 | 770+ сторінок |
| 11 | Фокус-пастка + Escape у модалках | 5.2 | користувач клавіатури зараз у пастці |
| 12 | Підігнати зображення під розмір показу + WebP | 2.2 | 1 475 КБ з 1 557 КБ ваги перегляду |

---

## 11. Відтворення

Нові скрипти цієї частини — у `tests/playwright/`:

```bash
node tests/playwright/crawl.mjs           # 699 URL sitemap + 614 внутрішніх посилань
node tests/playwright/categories.mjs      # усі 6 категорій × 2 версії сайту
node tests/playwright/counter.mjs         # лічильник «варіантів» проти реальних даних
node tests/playwright/a11y.mjs            # контраст WCAG + клавіатура, 5 сторінок × 2 версії
node tests/playwright/perf-viewports.mjs  # троттлінг CPU ×1/×4/×6 + матриця з 10 viewport-ів
node tests/playwright/pagetypes.mjs       # 10 додаткових сторінок (типи об'єктів, гід, статті)
node tests/playwright/cls.mjs             # джерела зсувів layout на головній
node tests/playwright/modal-desktop.mjs   # фокус-пастка модалок + десктопна стрічка
```

Обмеження те саме, що й у першій частині: Chromium у цьому середовищі не проходить через TLS-релей, тому запити сторінки виконуються через `context.route` + `fetch` Node (`tests/playwright/shim.mjs`). На DOM, CLS, коди відповідей і поведінку це не впливає; реальні мережеві розміри й `cache-control` заміряні окремо через `curl`. Зовнішні домени з цього середовища недоступні за політикою егресу — див. 6.4.
